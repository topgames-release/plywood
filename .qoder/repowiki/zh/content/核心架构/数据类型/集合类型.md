# 集合类型

<cite>
**Referenced Files in This Document**   
- [set.ts](file://src/datatypes/set.ts)
- [inExpression.ts](file://src/expressions/inExpression.ts)
- [attributeInfo.ts](file://src/datatypes/attributeInfo.ts)
- [dataset.ts](file://src/datatypes/dataset.ts)
- [set.mocha.js](file://test/datatypes/set.mocha.js)
</cite>

## 目录
1. [简介](#简介)
2. [核心实现](#核心实现)
3. [数据类型支持](#数据类型支持)
4. [集合运算](#集合运算)
5. [序列化与反序列化](#序列化与反序列化)
6. [与表达式交互](#与表达式交互)
7. [与数据集和属性信息交互](#与数据集和属性信息交互)
8. [性能优化建议](#性能优化建议)

## 简介
集合类型是Plywood中用于表示不可变元素集合的核心数据结构。它基于哈希表实现，提供高效的元素去重和查询操作。集合类型支持多种数据类型，包括字符串、数值、时间等，并提供丰富的集合运算功能，如并集、交集和差集。该数据结构在处理分类数据和维度过滤时发挥着重要作用。

**Section sources**
- [set.ts](file://src/datatypes/set.ts#L53-L475)

## 核心实现
集合类型采用基于哈希的内部实现机制，确保元素的唯一性和高效的查询性能。在构造函数中，通过`keyFn`函数为每个元素生成唯一键，并使用哈希表进行存储和去重。

```mermaid
classDiagram
class Set {
+static EMPTY : Set
+setType : PlyType
+elements : Array<any>
-keyFn : (v : any) => string
-hash : Record<string, any>
+union(other : Set) : Set
+intersect(other : Set) : Set
+contains(value : any) : boolean
+add(value : any) : Set
+remove(value : any) : Set
+toggle(value : any) : Set
}
class Set~SetValue~ {
+setType : PlyType
+elements : Array<any>
}
class Set~SetJS~ {
+setType : PlyType
+elements : Array<any>
}
Set --> Set~SetValue~ : "implements"
Set --> Set~SetJS~ : "implements"
```

**Diagram sources**
- [set.ts](file://src/datatypes/set.ts#L53-L475)

**Section sources**
- [set.ts](file://src/datatypes/set.ts#L53-L475)

## 数据类型支持
集合类型支持多种数据类型，包括字符串、数值、时间等。通过`setType`属性标识集合中元素的类型，并提供相应的类型转换和升级机制。

```mermaid
graph TD
A[基础类型] --> B[字符串]
A --> C[数值]
A --> D[时间]
A --> E[布尔值]
F[范围类型] --> G[字符串范围]
F --> H[数值范围]
F --> I[时间范围]
J[集合类型] --> K[SET/STRING]
J --> L[SET/NUMBER]
J --> M[SET/TIME]
J --> N[SET/BOOLEAN]
B --> K
C --> L
D --> M
E --> N
```

**Diagram sources**
- [set.ts](file://src/datatypes/set.ts#L53-L475)
- [attributeInfo.ts](file://src/datatypes/attributeInfo.ts#L0-L231)

**Section sources**
- [set.ts](file://src/datatypes/set.ts#L53-L475)
- [attributeInfo.ts](file://src/datatypes/attributeInfo.ts#L0-L231)

## 集合运算
集合类型提供丰富的集合运算功能，包括并集、交集、差集等操作。这些运算通过相应的方法实现，确保类型一致性并返回新的集合实例。

```mermaid
sequenceDiagram
participant Client
participant SetA
participant SetB
participant Result
Client->>SetA : union(SetB)
SetA->>SetA : 检查类型一致性
SetA->>SetA : 合并元素数组
SetA->>SetA : 执行unifyElements去重
SetA-->>Result : 返回新集合
Client->>SetA : intersect(SetB)
SetA->>SetA : 检查类型一致性
SetA->>SetA : 计算元素交集
SetA-->>Result : 返回新集合
Client->>SetA : contains(value)
SetA->>SetA : 使用哈希表快速查找
SetA-->>Client : 返回布尔值
```

**Diagram sources**
- [set.ts](file://src/datatypes/set.ts#L53-L475)

**Section sources**
- [set.ts](file://src/datatypes/set.ts#L53-L475)

## 序列化与反序列化
集合类型提供完整的序列化和反序列化支持，通过`toJS`、`fromJS`等方法实现JavaScript对象与集合实例之间的转换。

```mermaid
flowchart TD
Start([开始]) --> FromJS["fromJS(parameters)"]
FromJS --> CheckType{"参数类型检查"}
CheckType --> |数组| ConvertArray["转换为对象格式"]
CheckType --> |对象| ValidateObject["验证对象结构"]
ConvertArray --> ValidateObject
ValidateObject --> DetermineType["确定setType"]
DetermineType --> CreateElements["创建elements数组"]
CreateElements --> ConstructSet["构造Set实例"]
ConstructSet --> End([完成])
ConstructSet --> ToJS["toJS()"]
ToJS --> MapElements["映射elements数组"]
MapElements --> CreateJS["创建SetJS对象"]
CreateJS --> ReturnJS["返回JS对象"]
ReturnJS --> End
```

**Diagram sources**
- [set.ts](file://src/datatypes/set.ts#L53-L475)

**Section sources**
- [set.ts](file://src/datatypes/set.ts#L53-L475)

## 与表达式交互
集合类型在表达式中扮演重要角色，特别是在`inExpression`中用于成员资格检查。通过`contains`方法实现元素包含性判断。

```mermaid
sequenceDiagram
participant Expression
participant InExpression
participant Set
participant Operand
Expression->>InExpression : 创建in表达式
InExpression->>InExpression : 验证操作数类型
InExpression->>InExpression : 设置类型为BOOLEAN
InExpression->>Set : _calcChainableUnaryHelper
Set->>Set : contains(operandValue)
Set-->>InExpression : 返回布尔值
InExpression-->>Expression : 返回计算结果
```

**Diagram sources**
- [inExpression.ts](file://src/expressions/inExpression.ts#L0-L82)
- [set.ts](file://src/datatypes/set.ts#L53-L475)

**Section sources**
- [inExpression.ts](file://src/expressions/inExpression.ts#L0-L82)
- [set.ts](file://src/datatypes/set.ts#L53-L475)

## 与数据集和属性信息交互
集合类型与数据集和属性信息紧密集成，用于描述数据结构和类型信息。在数据集处理中，集合类型用于表示分类数据和维度过滤。

```mermaid
classDiagram
class Dataset {
+attributes : Attributes
+data : Datum[]
+getFullType() : DatasetFullType
}
class AttributeInfo {
+name : string
+type : PlyType
+nativeType : string
}
class Set {
+getType() : PlyType
+setType : PlyType
}
Dataset --> AttributeInfo : "包含"
AttributeInfo --> Set : "引用"
Set --> AttributeInfo : "提供类型信息"
Dataset --> Set : "数据元素"
Set --> Dataset : "作为数据值"
```

**Diagram sources**
- [dataset.ts](file://src/datatypes/dataset.ts#L0-L799)
- [attributeInfo.ts](file://src/datatypes/attributeInfo.ts#L0-L231)
- [set.ts](file://src/datatypes/set.ts#L53-L475)

**Section sources**
- [dataset.ts](file://src/datatypes/dataset.ts#L0-L799)
- [attributeInfo.ts](file://src/datatypes/attributeInfo.ts#L0-L231)
- [set.ts](file://src/datatypes/set.ts#L53-L475)

## 性能优化建议
对于大规模集合操作，建议采用以下内存管理策略：
1. 避免频繁创建和销毁集合实例
2. 利用`unifyElements`方法合并重叠的范围元素
3. 在必要时使用`simplifyCover`方法简化集合表示
4. 注意集合操作的时间复杂度，特别是交集和并集运算

```mermaid
flowchart TD
A[性能优化] --> B[避免频繁创建]
A --> C[合并重叠元素]
A --> D[简化集合表示]
A --> E[注意时间复杂度]
B --> F["重用集合实例"]
C --> G["调用unifyElements"]
D --> H["调用simplifyCover"]
E --> I["评估操作复杂度"]
F --> J[减少内存分配]
G --> K[降低元素数量]
H --> L[优化存储空间]
I --> M[选择合适算法]
```

**Diagram sources**
- [set.ts](file://src/datatypes/set.ts#L53-L475)

**Section sources**
- [set.ts](file://src/datatypes/set.ts#L53-L475)